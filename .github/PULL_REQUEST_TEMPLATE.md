## Summary

<!-- What does this PR do, and why? -->

## Pre-merge checklist

- [ ] If this touches auth/session/account_id (`lib/auth.ts`, `proxy.ts`,
      `lib/db/schema.ts`'s `accountId` columns, or `lib/ingest/auth.ts`),
      the decision is recorded in `specs/` — see AGENTS.md's "Specs &
      tickets".
